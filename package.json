{
  "name": "moovy",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @moovy/ui",
    "typecheck": "npm run typecheck -w @moovy/ui"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
